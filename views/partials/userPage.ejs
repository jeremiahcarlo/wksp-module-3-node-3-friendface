<div class="container">
    <div class="img-profile-box">
        <img class="avatar" src="<%= currentUser.avatarUrl %>">
        <h1><%= currentUser.name %> friends</h1>
    </div>
    </div>
  
  <%- include('friend') %>
  <%- include('notFriend') %>